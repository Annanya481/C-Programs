/* Jenny's hw for first day is to find addition of 2 numbers, help Jenny
to solve the problem.*/

#include<stdio.h>
int main()
{
    signed int a, b;
    scanf("%d %d", &a, &b);
    signed Sum = a+b;
    printf("The sum of 2 numbers: %d", Sum);
    return 0;
}